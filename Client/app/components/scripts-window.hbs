<div>
  <div class="tabs">
    <ul>
      <li {{on "click" this.toCreateScript}}><a>Créer un script</a></li>
      <li {{on "click" this.toListScript}}><a>Liste des scripts</a></li>
    </ul>
  </div>
  <div>
    
    {{#if this.isCreate}}
      <ScriptCreate @new_script={{@new_script}}/>
    {{/if}}
    {{#if this.isList}}
      List of scripts
    {{/if}}

  </div>
  {{yield}}
</div>

